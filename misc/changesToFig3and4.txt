Change for figure 3 — Make the labels explicit
Keep the three panels, but add a clear legend:
Background
P wave
T wave
Do not label QRS as a model class, because your model has only Background/P/T; QRS is mapped to Background. This is consistent with Section 3.1
Fix the wording "beat"
Your current figure shows more than one cardiac cycle, so don't call it:
“a QTDB validation beat”
Change to:
Representative QTDB validation ECG segment


Change for figure 4 — Make prediction interpretation obvious
Use:
Ground Truth (Background / P / T)
R3 Prediction (Background / P / T)
rather than leaving the colored bars unexplained.